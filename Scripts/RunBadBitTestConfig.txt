# Default configuration for RunBadBitTest.py
# Use key=value pairs. Lines starting with # or ; are ignored.

address=3
inter_packet_delay_ms=1000
test_stop_delay=1000
pass_count=1
# logging_level 0=none 1=minimal 2=verbose
logging_level=1
stop_on_failure=true
serial_port=COM6
in_circuit_motor=false
# press any key to continue or 'c' to trigger screen capture 
wait_key_press=true
# 32-bit flip mask, MSB-first across address/instruction/speed/checksum
# Special case: if flip_mask=0, all bits are tested starting at 0x80000000
# and shifting right one bit each test until all bits are tested.
flip_mask=0x00000000
