# Default configuration for RunStreamBitsTest.py
# Use key=value pairs. Lines starting with # or ; are ignored.
#
# Global parameters (serial_port, in_circuit_motor, logging_level) are read from SystemConfig.txt

# Locomotive address
address=3

# DCC timing and framing
preamble_bits=17
bit1_duration=58
bit0_duration=100
trigger_first_bit=true

# Number of idle packets inserted between start-speed packet and stop packet
# Set to 0 so stop is sent immediately after speed set.
idle_packet_count=5

# Delay after stream completes before checking stopped state (ms)
test_stop_delay_ms=500
